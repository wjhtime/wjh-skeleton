
var test_window = function () {
    //浏览器窗口
// log(window.innerHeight);
// log(window.innerWidth);

//navigator浏览器信息
    log(navigator.appName)

    log(screen.width)
    log(screen.height)


    log(location.href)
// location.reload()

    document.title = '日'

    log(document.cookie)

    log(history.length)
// history.back()
};